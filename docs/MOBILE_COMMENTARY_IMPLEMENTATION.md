# 双流移动端实现记录

本轮范围来自 [2026-09-20 研究](ANGELLIVE_DOUYU_MOBILE_RESEARCH_2026_09_20.md)：A 直播恢复、移动端双流引擎、系统音频状态、触摸校准与裁剪、后台生命周期，以及分平台 GitHub 测试包。保留全部已有功能。这里记录实现中的状态，不是发布公告。

## 基线与来源

- 开发基线 `0255cde502105e49e206d6a5602c9a5b75eb70d1`，含 Mac 三档解码候选；保留用户已验证的硬解路径。
- A EOF 无自动恢复：`upstream-existing`。本地冻结的 `origin/master` 为 `b97ed3ed628e6bf9dfacd96be3b4418da40eb966`，同样仅转发 `onComplete`。不是此次网络刷新后的上游最新版声明。
- iOS 默认 IJK 与双流同步接口冲突：`fork-regression`，入口开放但双流前置条件只接受 `SyncCapablePlayer`。首次无效状态在选择 B 后、尚未进入激活错误恢复块时产生。
- 校准暂时暂停 A 导致系统误报暂停：`integration-conflict`，单播放器系统音频状态订阅不能表达两路会话；暂停中断恢复和 duck 恢复满音量也需要会话级状态协调。
- 斗鱼静默超时误断：仍为 `not-reproduced`，未修改 135 秒策略和 15 秒重连间隔。不能把新代码当成这个假设已验证。
- 不复制 AngelLive 源码或引入其播放器依赖；按本项目现有接口独立实现已审阅的恢复行为。

## 已实现的基础

### A 的直播恢复

直播起播后收到 EOF 或 network/source 错误，使用独立恢复协调器合并通知，按 1、3、8 秒进行最多三次恢复。每次重新取房间状态和签名地址，优先按稳定清晰度 ID 保留选档；线路列表重排时优先匹配原 CDN，后续失败再尝试下一条新线路。确认下播停止，未知状态作为可重试失败；连续稳定播放 30 秒后才重置预算。

恢复不重建弹幕和页面控制器，向 UI 发布新画质/线路列表，并通知双流重新建立基线。公开播放、暂停、切房和关闭都会使旧恢复失效；原生开流完成后也检查最新暂停意图。录播、时移和 IPTV 不走该直播恢复路径。

### 双流引擎会话

启动双流时确保 A 有同步能力。iOS 当前 A 使用 IJK 时，先静音准备 MediaKit，准备完成才切换 A 所有权；失败或退出取消则销毁候选，保留原 A 和音量。双流会话内禁止自动/手动切到不兼容引擎。退出解除约束，当前 A 保持连续；下次重载或换房使用原内核偏好，不写永久设置。

Android 兼容模式的 `mediacodec` 配置现在只在 Android 生效，防止从备份导入该开关后误用于 iOS。

### 系统音频

新增 `LiveAudioSessionState`：会话播放流、输出音量、用户播放/音量操作代次。双流对外报告会话状态，校准单独暂停一路不再误停系统媒体状态。音频中断按序处理：只有中断前播放且没有后续用户命令才自动恢复；duck 按原音量比例降低并恢复，用户在中断期间修改音量则保留新值。显式停止清除恢复意图。

## 验证记录

- 第一轮恢复、来源解析、选流、双流定向测试：26 项通过。
- 增加真实 PlayerManager 假原生实例集成测试后，恢复、引擎切换、旧请求隔离与 Mac 解码配置定向回归：38 项通过。
- 最新音频中断、会话校准状态、引擎/恢复和媒体元数据测试：20 项通过。与上一组存在重叠，不将两组相加声称唯一用例数量。
- 本批 16 个受影响 Dart 文件定向静态检查无类型错误；发现的一个未使用导入和一个花括号提示已修正，对应文件复查 `No issues found`。文档本地链接与 `git diff --check` 通过。
- Flutter 3.47.0；没有本地打包、启动用户 App 或连接设备。首次测试因沙箱禁止测试器绑定回环端口未能启动，之后获工具执行许可后运行成功。
- 测试证明控制逻辑与回归断言；不证明真实网络、VideoToolbox/MediaCodec、移动端功耗或系统后台恢复已经通过实机验收。

## 校准连续性修复（插入优先处理）

用户反馈连续调延迟黑屏、校准后裁剪重新加载以及覆盖画质过低。本批修复记录见 [B 画面裁剪覆盖](COMMENTARY_VIDEO_OVERLAY.md#2026-09-20-校准连续性候选)。前台现在保持 B 视频轨，默认优先可读高清；移动端后台仍不能简单照搬旧的关轨策略。

- 校准、画质、视频子树、懒解析、偏移、覆盖、弹幕延迟、PlayerManager 恢复共 40 项定向测试通过。
- 再增加真实持续缓冲与手动选档边界后，控制器 10 项测试通过；包括短缓冲不重复应用偏移、真实断流恢复 A、选 8M 后裁剪不换源。该组与前组重叠。
- 最新 24 个改动 Dart 文件静态检查 `No issues found`。
- 候选版本 `3.0.21+4109`，先交付 Mac GitHub 测试包；不代表下列移动端工作已完成。

## 尚未完成

1. Android/iOS 前台、后台、锁屏与原生 Surface/PiP 的实机验收；代码入口已开放实验测试，不等同于设备验收完成。
2. iOS 原生 PiP 按研究中的独立验证阶段处理，目前没有实现，不在本次测试包内宣称支持。
3. 实机兼容性、长时间漂移与资源占用验收；云端质量门和三平台候选编译已完成，不能替代设备证据。
4. iOS 用户自行签名与安装后的运行反馈；普通未签名 IPA 已交付并完成下载后验证。

回滚以独立基础改动为单位：直播恢复、引擎约束、系统音频、后续移动界面分别检查；不回滚已经稳定的 Mac 三档解码。用户自有未跟踪 README 副本和 Podfile.lock 不纳入本轮提交。

## 共享触摸界面阶段

后续开发分支 `feat/mobile-commentary-20260920` 基于 Mac 候选 `57d507e2`；不修改正在构建的 Mac 候选分支。

- 移动端校准使用直播页面的可用区域，竖屏上下、横屏并排显示 A/B；没有创建第二个播放路由或重新解析源。画质、弹幕、重连、更换 B 仍从设置入口访问。
- 步长 10/100/500ms 加“声音提前/延后”，大字号及窄分屏下操作区可滚动。每个显示位置沿用挂载期内稳定的视频子树。
- 裁剪在移动端展开到页面；九点手柄和覆盖缩放触摸范围扩到 48px，桌面保持原尺寸。系统返回统一由既有路由级 BackScope 处理，编辑时先收起校准/取消裁剪，不嵌套第二套返回回调。
- `commentary_touch_calibration_test.dart` 覆盖 320×568、390×844、844×390、507×700、1024×768，以及 1.5 倍字体、真实步长回调、原生子树复用和触摸尺寸。与既有 BackScope 测试合并 6 项通过；独立裁剪旋转/确认用例通过。第一次布局测试暴露横向约束不足，已改为撑满各自画面区域后通过。
- 7 个改动 Dart 文件静态检查 `No issues found`。这些是可控 Widget 证据，尚无 Android/iOS 原生 Surface、温度和后台恢复实测；不据此开放或发布完整移动端双流版本。

## 移动会话与紧凑画面阶段（3.0.22+4110 候选）

- Android 入口开放实验测试，与 iOS 使用同一 MediaKit 双流约束；桌面解码选项不改动。
- 应用级移动生命周期观察器区分可见、系统 PiP、完全后台。`inactive` 不触发关轨，避免通知中心或分屏焦点变化破坏仍可见画面。进入后台按既有后台播放设置决定继续声音或暂停两路；返回只恢复由该观察器造成且没有被后续用户操作覆盖的暂停。
- 完全后台关闭两路视频轨，继续播放时保留音轨、实例与地址；前台/PiP 恢复视频，不重新解析地址，也不再次暂停以重复应用旧偏移。关闭视频轨后的原生时间关系不保证不变，恢复会提示再次核对；这与前台隐藏校准/切裁剪保持视频轨的策略不同。
- 后台事务串行，并按会话代次隔离切房/退出；退出等待事务后恢复 A，确保后台没有 B 残留。来电及 duck 仍由此前会话级中断协调器处理。
- Android PiP、应用内悬浮窗接入已确认的 B 裁剪层，小窗不显示编辑手柄，也不改变原校准/编辑状态。播放图标取会话状态，不再只跟随校准时可能短暂停顿的 A。移动端拖动不再调用桌面 window_manager API。
- iOS 主应用、Podfile、分享扩展部署目标统一为 15.6；分享扩展只用 UIKit/Social，没有 18.2 专属 API。保持扩展功能及现有 Bundle ID，不导入签名凭据。云编译仍需验证最低版本兼容；unsigned/TrollStore 产物不等于普通设备直接可安装。
- 合并定向回归 43 项通过（后台状态、切轨、退出恢复、PiP 展示、触摸、引擎恢复、系统中断和原有校准）；首次紧凑 Widget 测试需等待 GetX 重建落帧，改用 `pumpAndSettle` 后通过。

交付按 Mac 3.0.21 候选完成 → Android 3.0.22 → iOS 3.0.22 串行进行，全部应用打包在 GitHub，保持本地设备零操作。各包链接、SHA256 和具体运行结果以随后记录为准。

### iPhone/iPad 自行签名安装包

用户选择自行签名。云端在 TrollStore 专用签名前独立生成 `PureLive-*-ios-arm64-unsigned.ipa`，以 `Payload/Runner.app` 打包；`pure-live-ios-self-sign` 附件包含 IPA 与 SHA256，即使后续 TrollStore 步骤失败也保留普通 IPA。交付前检查 ZIP 完整性、设备平台标记、主程序、Flutter/AOT 二进制与资源，不把结构校验等同于签名或设备验收。签名工具仍需处理嵌入的分享扩展及所用账号支持的权限。

IPA 校验器进一步读取主程序、Flutter 和 AOT 的 Mach-O 架构头，要求存在真实 arm64 薄二进制或边界有效的 arm64 universal 切片；拒绝只有 x86_64、截断文件和伪造架构表。校验器 3 项 Python 测试通过。它不替代 Apple 签名、完整二进制可执行性或设备运行检查。

Android 仓库目前未配置正式签名 Secrets。本轮显式选择 `android_test_signing=true`，只交付名称带 `test-signed` 的候选 APK，使用云端临时 debug 证书。该证书不保证跨构建一致，不能覆盖不同证书的旧包；测试前应导出数据。此选项禁止与 `create_release=true` 同用；正式发布仍要求原有正式签名配置。

## 云端交付记录

- Mac `3.0.21+4109`：冻结提交 `57d507e2c7cfdd5aaa90b037830ffa93824835ee`，[运行 35515581371](https://github.com/shizuon/pure_live/actions/runs/35515581371) 成功。完整质量门、原生编译、应用/ZIP/DMG 校验和上传均通过；`hdiutil verify` 明确返回 VALID。[附件 pure-live-macos](https://github.com/shizuon/pure_live/actions/runs/35515581371/artifacts/10607481324)，222269219 字节，GitHub 上传归档 SHA256 为 `4c9bccfd997422a4b1ab6d7b18b7347cd5f6dddeb8776f987ed0ab6b92b4e2ae`。本机下载遇到 TLS/连接重置，下载后复核尚未完成；云端检查不代替用户实际播放测试。
- Android 首轮：冻结提交 `ea80da9c9e947c52a057a61da4a5b8b18112bceb`，[运行 35517965163](https://github.com/shizuon/pure_live/actions/runs/35517965163) 完整质量门成功，打包校验脚本误报版本；修复与最终产物见下文。
- iOS 与 Android 最终候选均已生成，见下文；尚无用户设备安装和播放成功证据。

Android 首轮后续结果：原生 APK 编译成功，内容检查及 APK v2 签名验证成功；新加的 manifest 校验把 Flutter build `4110` 误当成 split arm64 的 Android versionCode，因此报 `Wrong version`。Flutter 3.47 的 ABI 规则为 arm64 `2 × 1000 + build`，正确值是 `6110`。来源为本轮 CI 脚本回归，应用源码没有版本错误。修正校验器，并覆盖正确 split 值、错误 build/版本/包名/混合 ABI 的回归测试；打包校验 Python 共 5 项通过。后续仅重试打包，复用首轮已通过的静态检查与 528 项 Flutter 测试，不修改应用源码或重复执行全量门禁。

### Android 与 iOS 最终候选

- Android [运行 35519164786](https://github.com/shizuon/pure_live/actions/runs/35519164786) 全部成功，提交 `a7e064a8f7aed3cc5bd254db5b84246a72094229`。[测试包附件](https://github.com/shizuon/pure_live/actions/runs/35519164786/artifacts/10607124415) 已上传；包内 1259 项 Flutter 资源、15247346 字节，arm64 原生库、APK v2 签名、包名和 `3.0.22 / versionCode 6110` 均通过云端验证。
- `PureLive-3.0.22-4110-android-arm64-v8a-test-signed.apk` 的 SHA256：`60e87c4fdd14f889396e36fc2141517204809aeaf4fce7f36f61284b1162e903`。测试证书 SHA256：`d18d987cee8062a27c0da20d37b95aa0d44a2623b40d2a1b9b51dc0448099769`。本机下载后 `shasum -c` 与 `unzip -tq` 均通过。
- Android 完全结束后，同一提交的 iOS [运行 35519995189](https://github.com/shizuon/pure_live/actions/runs/35519995189) 全部成功。[普通未签名 IPA 附件](https://github.com/shizuon/pure_live/actions/runs/35519995189/artifacts/10608123252) 已上传；未创建正式 Release。
- `PureLive-3.0.22-4110-ios-arm64-unsigned.ipa` SHA256：`9589e9ea278a8aa2895610d22aa015122be6f37e7690b80f5dec7a045eda7863`。本机下载后哈希与 `verify_unsigned_ipa.py` 通过，确认 iPhoneOS、主程序和 Flutter/AOT arm64、版本 `3.0.22 (4110)`、资源完整；主应用和分享扩展的 `MinimumOSVersion` 均为 `15.6`。IPA 仍需用户自行签名，尚未在设备安装或运行。
- 使用步骤见 [移动端测试说明](MOBILE_COMMENTARY_TEST_GUIDE.md)。实机问题继续按反馈修复；编译和假播放器测试不证明 30 分钟漂移、发热、来电/蓝牙和锁屏恢复已达标。
